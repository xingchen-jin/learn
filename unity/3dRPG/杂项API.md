Camera.ScreenPointToRay摄像头射一条射线

public event Action<Vector3> OnMouseClicked;
//为什么前面有event?这是必须的吗？

鼠标右键加中键是什么?

Vector3.SqrMagnitude()算距离某些情况比distance方法的性能更优

Vector3.Dot 两个向量的点积
   点积是一个浮点值，它等于 将两个向量的大小相乘，然后乘以向量之间角度的余弦值。

Regidbody组件的is Kinematic勾选后可以避免与Nav Mesh Agent的一些冲突

LateUpdate每一帧被渲染后执行