
本笔记用于记录制作demo过程中出现的一些问题和对应解决思路
## 人物移动不自然-出现滑动
人物的位移和旋转我是通过刚体组件进行的，的确能起到位移的·作用。但这也引起了一个问题，我刚体位移的速度依赖设置的速度，与移动动画没有什么联动。当速度设置不恰当时，会很不自然。会感觉到平移。或是再严格点，当人物抬脚到落脚过程中位移仍不断进行，一点不自然。

ai给出的解决方案有：

1，启用animator的RootMotion。也就是根运动

2，代码驱动位移+手动匹配动画速度

3，混合RootMotion与代码。（选择性的启用）

4，使用AnimationRigging做脚步IK锁定

## 8月9号
1，确定游戏类型是射击游戏，导入Animation Rigging通过IK让动画匹配武器  
2，使用单混合树来进行动画由禁止到跑步的切换  
- 解决了脚本FSM过于复杂
- FSM状态机的站立，行走，跑步混成一个状态
- **隐患：混成一个状态可能后续不好区分**
3，使用PlayInput来接收输入，使用事件传递输入
4,新增托枪动画，使用Layer覆盖   

流萤Avator
![](assets/Pasted%20image%2020260809224625.png)




## 8月10号 
- 重构FSM为两层状态机

```Csharp
csharp

namespace Game.Player
{
    public class PlayerContext : MyFSM.Blackboard
    {
        public Animator animator;
        public CharacterController controller;
        public PlayerInputData input;   // 定义的输入结构体
        // 两个状态机引用（方便互相查询）
        public MovementFSM movementFSM;
        public WeaponFSM weaponFSM;
        // 可被WeaponFSM修改的速度上限
        public float maxSpeed = 5f;
        public bool canRun = true;
    }
}
```

## 8月11号
完成状态机框架，以及基本移动状态，托起枪，瞄准的状态，FSM状态机框架基本成型，特点是状态与切换条件，输入的解耦，输入信息通过上下文（黑板）通信。状态内部不处理转换逻辑，由状态机维护转换条件的字典。判断。基础状态通过引用进行包装至封装类，而不是继承，相当于玩家组件与状态机进行了桥接。
#### BUG集
- 角色脚步移动骨骼异常 (骨骼绑定的问题，需要检查动画和人物的Avator)
- 角色切换在瞄准，延迟过大，且不能正常切换 （Animator的Transiton忘记修改退出时间。且玩家输入一次后输出结果长时间保存，后续修改一下）
- 瞄准时八向移动，没有过渡（在脚本中添加过输入过渡时间）

## 8月13号
- 新增瞄准摄像机，越肩视角，并且可以跟随鼠标转动
- 新增准心，玩家通过rigging手部瞄准准心
- 完成开火功能，音效子弹，子弹会落在准心位置
- 新增对象池系统，目前右音效对象池和子弹对象池
###  问题
玩家瞄准的姿势不太自然  
过多的IK计算可能影响性能
### 后续规划
#### 先完善角色控制器
- 实现跳跃状态
- 实现蹲下状态
- 实现攀爬
- 实现翻墙
- 切换瞄准应该是玩家旋转到相机，而不该反过来

## 8月19号
- 手写重力
- 新增跳跃功能
- 新增下蹲功能
- 进入瞄准会自动旋转至屏幕中间
### 问题
-  跳跃不自然，衔接处打滑
- 下蹲动画没有rootmotion需要更换
- 移动时开火，子弹偏移，目测为上一帧
- 跳跃指令会在不该跳跃时保存，导致切换回可以跳跃时会一下子起跳，这是输入管理在状态类外的玩家脚本处理的弊端之一，需要额外进行输入管理，不过做好了可以有更高的自由度
### 明日任务
- [x] 解决跳跃衔接打滑
（混合树在跳跃状态时根据速度确定播放什么动画，当速度为0时是最高点，落地时会赋值速度为瞬间赋值为0，导致动画在最后一帧切换到top的片段又迅速退出状态，导致动画过渡不自然） 
- [x] 完成下蹲动画
- [x] 完善下蹲状态，碰撞体调整
- [x] 解决开火问题

## 8月20号
- 下蹲时从根据头的位置实时计算高度和预定高度，考虑性能用了后者
- 完善状态接口，加入LateUpdate，改用继承抽象类，抽象类继承接口，使得代码更简洁
- 注意骨骼动画的违和，IKpASS会跟rigging起冲突，后期优化
- 了解抽象和虚拟大致区别
## 1. `abstract` 与 `virtual` 的核心区别

|特性|`abstract` 方法|`virtual` 方法|
|---|---|---|
|**是否有实现**|没有实现体，只有方法签名|有实现体（可以是空实现）|
|**派生类是否必须重写**|**必须**使用 `override` 重写，否则派生类也必须是抽象类|**可选**重写，若不重写则使用基类实现|
|**能否在基类中调用**|不能直接调用，因为无实现|可以调用，执行基类实现或派生类重写|
|**适用场景**|强制子类提供具体行为，例如每种状态都必须有 `OnEnter` 逻辑|提供默认行为，子类按需覆写，例如大多数状态可能不需要 `OnFixedUpdate`|
### 明日任务
- [x] 加入翻墙状态
## 8月21，22号
- 使用射线检测，完成翻墙的检测（低位攀爬，高位攀爬，翻越）
- 使用MatchTarget对齐角色翻墙高度
	- 踩坑：matchTarget必须在动画非过渡状态且必须要在选定的帧数才能动，不然不能正常动，而且bug一堆
- 角色控制器先告一段落
#### 感悟
3d角色控制器用程序适应动画，真tm的麻烦，找bug不能太急，让ai解决matchTarget的异常，ai做程序有时候会乱找原因（想当然的），所以使用ai需求分析应该明确点。


#### 开学了，这期间进度很慢，回去补了点基础，规划了独立游戏制作，以及报名了国内的一个21天GameJam。目前在独立游戏策划阶段，之后会很累，需要效率点了
#### 任务(1天)
- [x] 实现敌人数据
- [x] 实现敌人脚本，收到伤害后的处理
- [x] 完善子弹逻辑，子弹应该至少具有伤害，速度，来源这些信息
#### 敌人行为树初步（9-6到9-13）
- [ ] 学习行为树，准备敌人ai模块制作，实现最基础的行为树
- [ ] 让敌人至少完成巡逻-追击-攻击-死亡的行为与转换
#### 敌人生成逻辑（9-13至9-15）3天
- [ ] 敌人对象池生成（可刷新敌人）
- [ ] 可控的生成，生成器提供指定怪物，数量，位置的接口
- [ ] 场景固定敌人（辅功能，做到不与上面冲突）
- [ ] 敌人寻路制作
主要表现
- [ ] 主要实现敌人在一定范围内生成，巡逻，并攻击玩家的循环
#### 背包系统与 UI Toolkit 物品编辑器（6天，9-21截止）
- [ ] 背包UI搭建
- [ ] 物品管理器制作（与so联立）
- [ ] 背包实现增删改查
主要表现
- [ ] 完成物品拾取功能

#### 任务系统与主菜单/UI 完善（6天 - 9-27截止）
- [ ] 复用行为树，制作友方npc
- [ ] 实现任务触发载体（部分rpg是npc，我玩的很多是按地点）
- [ ] 实现任务数据，任务内容与奖励，以及触发任务的前提条件，以及是否触发，是否重复触发（可以归为前提条件制作）
- [ ] 完成内容制作
- [ ] 奖励获取
- [ ] 前提条件规划
主要表现
- [ ] 完成玩家截取任务 - 执行任务 - 完成任务的循环

#### 音乐与音效的加入（3天- 9-30截止）
- [ ] 基于并完善做好的音效池以及音效管理器，做好行走音效
- [ ] 换弹音效
- [ ] 爬墙音效
- [ ] ui点击音效


## 好了进入，顺利的话进入下一个月，10月1号国庆，聚光灯开赛，接下来21天开发核心在它

### 21天期间必须完成
- [ ] 角色动作调整，手感需要顺滑，打磨好
- [ ] demo打磨，上传第一期视频
- [ ] 学完唐老狮的网络游戏开发
- [ ] 背八股

## 比赛结束后
- [ ] 制作简历 
## demo后续制作规划
#### 重构实现热更新

#### 网络联机模块制作
