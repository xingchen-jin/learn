## 随着时间变化刷新地图显示内容
当玩家挖坑浇水这样对地图瓦片信息更改，应该需要把这些信息更新进字典，便于后续判断
同时需要加载场景信息在加载场景后，视频采取了把所有信息遍历（O（n）），判断是否更改信息后进行重新设置。

同时当游戏时间推进一天也要进行地图信息的更新，这里就可以添加一个时间传入天数和季节
在时间系统中天数推荐时呼叫，在地图瓦片管理器中注册，依旧遍历字典做判断。
天数过一天执行刷新当前场景的地图，其余信息在场景切换时刷新。

## 种子数据库制作
 创建Crop相关代码，视频创建crop-data文件夹
 创建CropDetails，存放种子id，不同阶段生长周期时间以及Prefab以及对应图片，
 可种植季节，收割工具信息，每种工具使用所需次数
创建对应的SO文件，CropDataList_SO

## 实现播撒种子的事件

 首先先创建种植管理器
 对于种植需要判断两个条件，挖完坑而且瓦片上没有种子/种植物
 同时种子种下去需要创建相关object，跨代码执行的话，创建一个事件PlantSeedEvent传入ID和物品信息

## 种子成长过程
为Crop预制体创建脚本，暂时只存itemdetails
为实现种子成长，那么就需要随着天数刷新，生成的农作物。
我们可以使用在RefreshMap()函数中，对原本农作物进行删除，然后呼叫PlantSeedEvent实现刷新，同时在天数事件中添加如果有农作物种植天数自增。

## 实现菜篮子收割庄稼行为
为实现这个功能，我想着应该拆分成，收割前置条件，收割过程以及收割后，具体可以拆成玩家选中工具，判断是否满足收割，播放收割动画，农作物本身对应处理（消失，耐久-1...），玩家背包增加物品五部分。
**Physics2D.OverlapPointAll**传入点返回周围碰撞体数组