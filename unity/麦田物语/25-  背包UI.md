## 制作ActionBar UI
创建UI场景，创建画布,再创建一个空物体作为容器相关ui的整体。
创建image作为物品栏主体，挂载Horizontal LayOut Group组件管理子物体
创建一系列button设定好大小图片文字。
再创建背包图片的image，挂载Layout Element，勾选ignore Layout使得它不受父物体管理。

## SlotUI 根据数据显示图片和数量

#### Navigation可以选择被键盘如何控制，none为不被键盘控制。

创建SlotUI脚本，获取slotImage与amountText。
再Inspector窗口中获取要比awake中更早。


## 背包UI显示

使用事件而非单例，减少耦合
```csharp
public static class EventHandler

{

    public static Action<InventoryLocation, List<InventoryItem>> UpdateInventoryUI;

  

    public static void CallUpdateInventoryUI(InventoryLocation location, List<InventoryItem> list)

    {

        UpdateInventoryUI?.Invoke(location, list);

    }

}
```
