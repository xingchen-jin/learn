## 构建时间系统
### 1，先在PersistentScene中创建空的gameObject并取名为TimeManager
### 2，实现对应脚本
先定义好，代表时间的变量gameSecond,gameMinute,gameHour,gameDay,gameMonth,gameYear;

再创建枚举
`` public enum Season{春天,夏天,秋天,冬天}
定义季节

在Settings代码中设定好时间相关的值，比如游戏秒的阈值大小，每分钟最大秒数等等

在timeManager脚本中使用简单的计时器记录秒，并更新游戏时间
```csharp
    private void Update()

    {

        if(!gameClockPause)

        {

            tikTime += Time.deltaTime;

            if(tikTime >= Settings.secondThreshold)

            {

                UpdateGameTime();//自定义函数，更新游戏时间

                tikTime -= Settings.secondThreshold;

            }

  

        }

    }
```

## 时间系统UI

#### 1，创建ui的object
先在main Canvas中创建一个Panal，取名为Game Time
定位在右上角，并给予时间背景图片（ui_time_16）。

#### 2，创建空子物体，显示时间切换，命名Day & Night
给予纯黑色的圆形作为背景图
再创建一个image子物体，并在Day & Night添加mask组件进行蒙板。

#### 3，创建空子物体，显示时间进度条，命名Clock

添加一系列Image子物体，命名Time Block。这些子物体显示的就是进度条的每一小部分。（通过控制这些子物体的显示实现进度条效果）
#### 4，创建text文本子物体和image子物体，显示具体时间，命名Game Date和Time
调整大小时由于，文本位置时通过美术资源固定，所以大小需要手动调整至需要大小
image用给好的ui_time_18作为背景

#### 5，创建image子物体，显示季节，命名Seaon Image

图片资源用给好的ui_time_11作为季节背景,再创建一个image子物体显示具体季节（ui_time_12春天）

#### 6，添加buttom子物体，显示暂停按钮
 **在图片中设置Advanced-Read/Write为true**。可以使对背景透明的图片的射线检测只检测有实际图像的部分
